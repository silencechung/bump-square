# Structure (意圖結構樹)

agent 整理完的結果。兩個 tab:

- **Tree** — 可收合的結構樹(圓形 +/− toggle、連接線)
- **Prompt** — markdown 渲染,「預覽 / 原始碼」toggle 可編輯。內容 = `## 結構`(code-fence 樹狀)+ `## 節點說明` + 選配 `## Assets 生成 prompt`

這份 markdown 就是要存檔、複製路徑、交給下游的 **spec**。
