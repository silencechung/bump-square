# Structure (intent tree)

The agent's output. Two tabs:

- **Tree** — collapsible structure tree (round +/− toggles, connector lines)
- **Prompt** — markdown render with a "preview / source" toggle, editable. Content = `## 結構` (code-fence tree) + `## 節點說明` + optional `## Assets 生成 prompt`

This markdown is the **spec** — the thing to save, copy the path of, and hand off downstream.
