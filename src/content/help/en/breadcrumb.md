# Workflow: three steps

`upload → layout → structure`

The chevron shape hints direction. Completed steps can be revisited; locked steps stay greyed out (no image → nothing to frame; no structure → nothing to view).
