# 存檔 + 路徑複製

**💾 存檔** — 把目前 board 存成具名 JSON,放在 `~/.bump-square/saves/`。多組可以並存切換。

**複製路徑** — 把最新 save 的絕對路徑放剪貼簿,貼給下游 reader skill / 開發 agent / 朋友讀檔。

bump-square 的 handoff 就是這個動作 — 沒有「自動送交」按鈕,意圖的下游由你決定。
